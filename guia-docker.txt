sudo apt-get update -y && sudo apt-get upgrade -y

cd ~

mkdir docker

cd docker

sudo apt-get install docker docker-compose

docker pull mysql:8.0

docker pull phpmyadmin/phpmyadmin

docker pull php:7.4-apache

PEGAR ARCHIVO - docker-compose.yml

mkdir db_data

mkdir web

docker-compose up -d